import 'package:flutter/material.dart';

class Preidict_Price extends StatelessWidget {
  const Preidict_Price({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('예상 판매 가격 & 에상 수익'),
      ),
      body:
      
    
    );
  }
}